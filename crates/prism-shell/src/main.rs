//! `prism-shell` — the interactive client.
//!
//! Readline-based REPL with multi-line input, connection management, and output
//! formatting per model. Depends only on `prism-protocol`. See
//! `docs/specs/shell.md`.

fn main() {
    eprintln!("prism-shell: not yet implemented (Phase 4 / M4).");
    std::process::exit(1);
}
