//! `prism-bench` — the benchmark harness.
//!
//! Synthetic workloads (insert-heavy, read-heavy, mixed) driven through the
//! embedded API, with latency and throughput reporting in comparison-friendly
//! formats. See `docs/operations/benchmarking.md`.

fn main() {
    eprintln!("prism-bench: not yet implemented.");
    std::process::exit(1);
}
