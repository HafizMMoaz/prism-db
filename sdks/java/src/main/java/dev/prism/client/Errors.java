package dev.prism.client;

/**
 * Placeholder holder; the error types live in their own files so each can be
 * {@code public}: {@link PrismException}, {@link ProtocolException},
 * {@link ServerException}, {@link ErrorInfo}, {@link ErrorCode}.
 */
final class Errors {
    private Errors() {}
}
